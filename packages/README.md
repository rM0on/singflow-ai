# SingFlow AI Packages

This directory is reserved for future shared packages such as API type contracts,
design tokens, or shared utilities.

Phase 0 intentionally does not add shared business logic here.
